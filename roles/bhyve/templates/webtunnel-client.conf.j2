loader="uefi"
cpu=1
memory=1G
network0_type="virtio-net"
network0_switch="{{ vm.networking.lan_interface }}"
disk0_type="virtio-blk"
disk0_name="disk0.img"
network0_mac="{{ webtunnel[os].mac_address }}"