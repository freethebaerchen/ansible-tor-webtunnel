server {
        listen 80;
        listen [::]:80;

        server_name _;

        location /.well-known/acme-challenge/ {
                root {{ webserver.webroot }};
        }

        location / {
                return 301 https://$host$request_uri;
        }
}