---

- name: Reload daemon
  ansible.builtin.systemd:
    daemon_reload: true

- name: Start tor
  ansible.builtin.service:
    name: tor
    state: started
    enabled: true

- name: Restart tor
  ansible.builtin.service:
    name: tor
    state: restarted
    enabled: true
